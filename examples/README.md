# Recipe / Workflow 示例

本目录存放示例的 Recipe / Workflow YAML 声明，供文档、测试与 Marketplace 参考。

结构规划：

```
examples/
├── recipes/
│   ├── system.cpu.yaml
│   ├── system.disk.yaml
│   └── docker.status.yaml
└── workflows/
    ├── deploy.dotnet.yaml
    └── deploy.node.yaml
```

首批示例将在 v0.2 引入 Recipe / Workflow Engine 时补齐。
