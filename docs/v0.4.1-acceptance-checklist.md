# v0.4.1 验收清单 — GA 工程化收尾

- 状态：Released（v0.4.1）
- 目标版本：v0.4.1
- 更新日期：2026-07-11
- 范围：只处理版本、文档、SDK 契约、安装验收和配置迁移，不新增产品功能。

## 1. 统一版本源

- [x] 根目录 `VERSION` 是发布版本权威来源
- [x] `sdk/version.Version` 供所有 Go module 读取
- [x] CLI `mow version` 输出统一版本
- [x] Desktop 标题与 Wails `App.Version` 使用统一版本
- [x] SSH / Docker / AI 官方插件 Metadata 使用统一版本
- [x] 前端 `package.json` / lockfile 同步为 0.4.1
- [x] Release Workflow 校验 tag 与 `VERSION` 一致
- [x] Release build 用 ldflags 注入 tag 对应版本

## 2. 文档一致性

- [x] README、CHANGELOG、roadmap 记录 v0.4.1 收尾范围
- [x] v0.4 已发布状态不再残留旧“进行中”描述
- [x] 详细开发计划链接本验收清单
- [x] Release notes 能解析 v0.4.1 验收清单

## 3. SDK 契约测试

- [x] Plugin Metadata / CommandSpec 静态校验
- [x] 稳定错误码、retryable、details 与 `errors.Is`
- [x] AI Chat / ToolCall JSON 字段与往返
- [x] Permission / Caller / Signal / Health 枚举转换
- [x] Plugin handshake 常量
- [x] Connection 凭据信封往返
- [x] gRPC error 转换契约

## 4. 安装与启动 Smoke

- [x] 提供 Unix Release archive 安装脚本
- [x] 提供 Windows Release archive 安装脚本
- [x] 校验 CLI 与 AI Plugin 压缩包 checksum
- [x] 解压到临时安装目录并按运行时约定命名插件
- [x] 执行 `mow version`
- [x] 使用临时配置执行 `mow ai providers` 并发现 mock Provider
- [x] Release Workflow 在 Linux amd64、Windows amd64、macOS arm64 运行 Smoke
- [x] Release 创建必须等待全部 Smoke 通过

## 5. 配置迁移

- [x] 配置文件新增 schema `version`
- [x] 无版本的 v0.3/v0.4 配置迁移到当前版本
- [x] 保留 app、logger、plugin settings
- [x] 新增 AI 配置使用安全默认值
- [x] 拒绝高于当前支持版本的配置
- [x] 迁移后 Save / Load 往返通过

## 6. 发布门槛

- [x] GitHub CI 全绿
- [x] 三平台 Release Smoke 全绿
- [x] tag `v0.4.1` 与根 `VERSION` 一致
- [x] GitHub Release 包含 CLI、SSH、Docker、AI 和 checksum
